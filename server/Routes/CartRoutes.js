import express from "express";
import {AddToCart} from "../Controllers/CartController.js";

const router=express.Router();

// router.post('/AddToCart/:userId/:itemId',AddToCart);

export default router